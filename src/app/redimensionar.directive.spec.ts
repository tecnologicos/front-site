import { RedimensionarDirective } from './redimensionar.directive';

describe('RedimensionarDirective', () => {
  it('should create an instance', () => {
    const directive = new RedimensionarDirective();
    expect(directive).toBeTruthy();
  });
});
