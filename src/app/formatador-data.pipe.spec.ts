import { FormatadorDataPipe } from './formatador-data.pipe';

describe('FormatadorDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatadorDataPipe();
    expect(pipe).toBeTruthy();
  });
});
